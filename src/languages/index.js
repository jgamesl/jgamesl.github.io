import de from './de.json';
import en from './en.json';
import es from './es.json';

export const dictionaryList = { en, es, de };

export const languageOptions = {
  en: 'English',
  de: 'Deutsch',
  es: 'Español'
};